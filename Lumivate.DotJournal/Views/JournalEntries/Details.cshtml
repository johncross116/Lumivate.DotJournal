@* TODO-dotjournal step 5: Add the @model directive for JournalEntry *@
@* @model Lumivate.DotJournal.Models.JournalEntry *@

@{
    ViewData["Title"] = "Journal Entry";
}

@* TODO-dotjournal step 5: Uncomment the block below to display the full journal entry. Fill in the Razor expressions where marked with TODO (inside HTML comments). *@
@*
<h1>@Model.Title</h1>

@if (!string.IsNullOrEmpty(Model.Mood))
{
    <span class="badge bg-info text-dark mb-3">
        <!-- TODO-dotjournal step 5: Display the Mood -->
    </span>
}

<p class="text-muted">
    <!-- TODO-dotjournal step 5: Display the date: @Model.CreatedAt.ToString("MMMM dd, yyyy h:mm tt") -->
</p>

<div class="card">
    <div class="card-body">
        <p style="white-space: pre-wrap;">@Model.Content</p>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Edit</a>
    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Delete</a>
    <a asp-action="Index" class="btn btn-secondary">Back to Journal</a>
</div>
*@
